require 'spec_helper'

describe "Allowables::ActiveRecord::Context" do
  describe "allowed?" do
    pending "should require a subject and a role object or slug"
    pending "should wrap Subect#has_role?"
  end

  describe "allowed_to?" do
    pending "should require a subject and a permission object or slug"
    pending "should wrap Subect#has_permission?"
  end
end
